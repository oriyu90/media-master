# Media Master requires no secret keys.
# (The AI Studio scaffold's GEMINI_API_KEY and the unused Firebase AI / AppCheck
# dependencies were removed in v1.1.0. This file stays as a placeholder so local
# tooling that expects it keeps working.)
